#! /bin/sh

# Prepare a freshly checked out copy of
# the CVS tree for compilation.

# $Id: prepare,v 1.1.1.1 2004-11-10 17:44:38 ianb Exp $

rm -f config.cache && echo -n "." &&
autoheader && echo -n "." &&
aclocal && echo -n "." &&
automake && echo -n "." &&
autoconf && echo -n "." &&
echo
